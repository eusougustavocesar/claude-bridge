import { PageStub } from "@/components/page-stub";

export default function ConfigPage() {
  return (
    <PageStub
      title="Configuration"
      description="Shipping in Day 3 — form to edit .env values via the API."
    />
  );
}
