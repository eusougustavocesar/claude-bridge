import { PageStub } from "@/components/page-stub";

export default function LogsPage() {
  return (
    <PageStub
      title="Audit log"
      description="Shipping in Day 4 — viewer for logs/audit.jsonl with live tail."
    />
  );
}
