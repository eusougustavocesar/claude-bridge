import { PageStub } from "@/components/page-stub";

export default function PairPage() {
  return (
    <PageStub
      title="Pair a phone"
      description="Shipping in Day 3 — will render the pairing QR from the daemon with live status updates."
    />
  );
}
